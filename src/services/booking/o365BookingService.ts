import axios from "axios";

export async function createBooking(phone: string) {

  // Placeholder until Azure fully active
  // This should call Microsoft Graph

  return {
    success: true,
    message: "Booking link sent via SMS."
  };
}
